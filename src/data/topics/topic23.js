import { Layers } from 'lucide-react';

export default {
  id: "task-23",
  title: "Задание 23: Количество программ исполнителя",
  description: "Сколько программ переводят число A в B — решаем короткой рекурсией.",
  icon: Layers,
  theory: `### Суть задания

Исполнитель преобразует число на экране. У него есть **команды** (например «Прибавить 1», «Прибавить 3», «Умножить на 2»). **Программа** — это последовательность команд. Нужно посчитать, **сколько различных программ** переводят число **A** в число **B**. Часто добавляют условия: траектория **должна** или **не должна** содержать какое-то число.

### Команда и программа — это разные вещи

- **Команда** — одно действие (+1, +3, ×2…).
- **Программа** — цепочка команд.

Поэтому и вопросы бывают двух видов:
1. **«Сколько программ»** переводят A в B (любой длины) — базовый случай.
2. **«Сколько программ ровно из N команд»** или **«какие числа достижимы за N команд»** — тут уже важна **длина** программы (число команд). Это решается тем же методом, но с дополнительным счётчиком команд (см. ниже).

### Универсальный метод — короткая рекурсия

Пишем функцию \`f(c, e)\` — «сколько программ переводят текущее число **c** в конечное **e**»:

\`\`\`python
def f(c, e):
    if c > e: return 0        # проскочили мимо цели — тупик
    if c == e: return 1       # дошли ровно — это одна готовая программа
    return f(c+1, e) + f(c+3, e)   # пробуем каждую команду и складываем
print(f(3, 20))
\`\`\`

Как читать:
- \`c == e\` → мы пришли в цель, это **один** успешный путь → \`return 1\`.
- \`c > e\` → перескочили цель, отсюда уже не вернуться → \`return 0\`.
- иначе пробуем **каждую команду** (+1 и +3) и **складываем** результаты — это и есть «количество способов».

### Запрещённое число (траектория НЕ содержит K)

Добавляем условие в самое начало — если попали в K, путь тупиковый:

\`\`\`python
def f(c, e):
    if c > e or c == 10: return 0   # 10 запрещено
    if c == e: return 1
    return f(c+1, e) + f(c+3, e)
\`\`\`

Любая траектория, наступившая на запрещённое число, сразу даёт 0 и в сумму не попадает.

### Обязательное число (траектория содержит K) — перемножаем!

Если программа **обязана пройти через** K, разбиваем путь на два независимых куска и **перемножаем** их количества:

\`\`\`
программ(A → B через K) = f(A, K) × f(K, B)
\`\`\`

Почему именно **умножение**: каждый путь от A до K можно продолжить **любым** путём от K до B — это все возможные комбинации, а число комбинаций — это произведение. Эту логику ты и видишь в \`print\`: сначала считаем до обязательной точки, потом от неё, и перемножаем.

#### Разбор примера (задание 23.9)

![Задание 23.9 — условие](/tasks/23/ex-23-9.png)

Команды **+1** и **+3**. Из **3** в **20**, траектория **содержит 15** и **не содержит 10**.

\`\`\`svg
<svg viewBox="0 0 720 260" xmlns="http://www.w3.org/2000/svg" style="max-width:100%;height:auto">
  <defs><style>
   .bg{fill:#0f172a}
   .node{fill:#1e293b;stroke:#475569;stroke-width:1.5}
   .nk{fill:#1e1b4b;stroke:#a78bfa;stroke-width:2.5}
   .nz{fill:#3b1e1e;stroke:#f87171;stroke-width:2}
   .nt{fill:#0d2d1a;stroke:#4ade80;stroke-width:2.5}
   .vn{fill:#e2e8f0;font:bold 14px sans-serif;text-anchor:middle;dominant-baseline:middle}
   .br{stroke:#64748b;stroke-width:2;fill:none}
   .f1{fill:#60a5fa;font:bold 14px sans-serif;text-anchor:middle}
   .f2{fill:#fbbf24;font:bold 14px sans-serif;text-anchor:middle}
   .res{fill:#4ade80;font:bold 18px sans-serif;text-anchor:middle;dominant-baseline:middle}
   .cap{fill:#94a3b8;font:11px sans-serif;text-anchor:middle}
  </style></defs>
  <rect width="720" height="260" class="bg" rx="8"/>
  <text x="360" y="28" class="cap">Программа обязана пройти через 15 (и не содержать 10). Команды: +1, +3. Из 3 в 20.</text>
  <circle cx="110" cy="110" r="22" class="nt"/>
  <text x="110" y="110" class="vn">3</text>
  <circle cx="300" cy="110" r="22" class="nz"/>
  <text x="300" y="110" class="vn">10</text>
  <circle cx="470" cy="110" r="22" class="nk"/>
  <text x="470" y="110" class="vn">15</text>
  <circle cx="630" cy="110" r="22" class="nt"/>
  <text x="630" y="110" class="vn">20</text>
  <line x1="132" y1="110" x2="278" y2="110" stroke="#334155" stroke-width="1.5"/>
  <line x1="322" y1="110" x2="448" y2="110" stroke="#334155" stroke-width="1.5"/>
  <line x1="492" y1="110" x2="608" y2="110" stroke="#334155" stroke-width="1.5"/>
  <text x="300" y="150" class="cap" fill="#f87171">запрещено</text>
  <path d="M 110 145 L 110 165 L 470 165 L 470 145" class="br"/>
  <text x="290" y="190" class="f1">f(3, 15) = 24</text>
  <path d="M 470 145 L 470 175 L 630 175 L 630 145" class="br"/>
  <text x="550" y="200" class="f2">f(15, 20) = 4</text>
  <text x="360" y="235" class="res">24 × 4 = 96 программ</text>
</svg>
\`\`\`

\`\`\`python
def f(c, e):
    if c > e or c == 10: return 0   # запрет 10
    if c == e: return 1
    return f(c+1, e) + f(c+3, e)
print(f(3, 15) * f(15, 20))   # 24 * 4 = 96
\`\`\`

**Ответ: 96.** Запрет 10 «живёт» внутри \`f\`, поэтому он автоматически отсекается на участке 3→15 (на участке 15→20 числа 10 и так нет).

### Увеличение и уменьшение — следи за знаком

Если команды **увеличивают** число (+1, ×2…), идём снизу вверх, и тупик — это «перескочили цель»: \`c > e\`.

Если команды **уменьшают** число (−1, −3, целочисленное деление…), всё **зеркально**: стартовое число *больше* целевого, идём сверху вниз, и тупик — это \`c < e\`. То есть знак в базовом условии меняется на противоположный:

\`\`\`python
# уменьшающие команды (−1, −3): из большого A в маленькое B
def f(c, e):
    if c < e: return 0        # проскочили вниз мимо цели
    if c == e: return 1
    return f(c-1, e) + f(c-3, e)
\`\`\`

Главное правило: **в сторону движения** числа должны приближаться к цели, а условие «проскочили» ставит знак в обратную сторону.

### Вопрос про число команд (а не просто «сколько программ»)

Когда спрашивают «**ровно за N команд**» или «какие числа достижимы за N команд», добавляем счётчик команд \`k\` (растёт на каждый вызов = одна команда):

![Калькулятор +1, +5, ×3 — 8 команд](/tasks/23/ex-8commands.png)

\`\`\`python
# Калькулятор: +1, +5, ×3. Сколько чисел из [1000,1024]
# достижимы из 1 программой ровно из 8 команд?
def f(c, e, k):
    if c > e or k > 8: return 0
    if c == e: return k == 8      # дошли — но засчитываем, только если команд ровно 8
    return f(c+1, e, k+1) + f(c+5, e, k+1) + f(c*3, e, k+1)

print(sum(1 for x in range(1000, 1025) if f(1, x, 0)))   # 1
\`\`\`

Здесь \`k\` — длина программы. База \`c == e\` теперь возвращает не просто 1, а проверку \`k == 8\` (нужна программа именно из 8 команд). А \`f(1, x, 0)\` для каждого x проверяет, достижимо ли это число.

### Память о предыдущей команде — флаг, а НЕ строка

Иногда есть условие на **последовательность** команд, например «нельзя две команды умножения подряд». Тогда надо помнить предыдущую команду.

![Калькулятор +1, +2, ×2 — без двух умножений подряд](/tasks/23/ex-nomult.png)

Соблазн — таскать строку из выполненных команд \`s\` и проверять \`'33' in s\`:

\`\`\`python
# РАБОТАЕТ, но МЕДЛЕННО — строка ломает кэш
def f(c, e, s):
    if c > e or '33' in s: return 0
    if c == e: return 1
    return f(c+1, e, s+'1') + f(c+2, e, s+'2') + f(c*2, e, s+'3')
print(f(1, 15, ''))   # 1545 — на маленьких числах ок, на больших зависнет
\`\`\`

Проблема: у каждого пути своя уникальная строка \`s\`, поэтому состояния **никогда не повторяются** и \`@lru_cache\` бесполезен. На больших числах это намертво виснет.

Правильно — хранить **маленький флаг** «предыдущая команда была умножением». Тогда состояний мало, они повторяются, и кэш реально ускоряет:

\`\`\`python
from functools import lru_cache

@lru_cache(None)
def f(c, e, mult_last):
    if c > e: return 0
    if c == e: return 1
    total = f(c+1, e, False) + f(c+2, e, False)
    if not mult_last:                 # ×2 разрешено, только если прошлая команда — НЕ ×2
        total += f(c*2, e, True)
    return total

print(f(1, 15, False))    # 1545
print(f(1, 150, False))   # огромное число — но считается мгновенно благодаря кэшу
\`\`\`

\`mult_last\` принимает всего два значения, поэтому одинаковые состояния \`(c, e, флаг)\` повторяются и \`@lru_cache\` их переиспользует. **Вывод: всё, что нужно помнить о прошлом, кодируй маленьким флагом/числом, а не растущей строкой — иначе кэширование не работает.**
`,

  practice: [
    {
      question: "Что возвращает рекурсия f(c, e) в случае c == e?",
      options: [
        { id: "A", text: "0 — это тупик" },
        { id: "B", text: "1 — дошли до цели, это одна готовая программа" },
        { id: "C", text: "c" },
        { id: "D", text: "f(c+1, e)" }
      ],
      correct: "B",
      explanation: "c == e означает, что текущее число совпало с целью — найдена одна успешная программа. Поэтому возвращаем 1, и эти единицы суммируются по всем путям."
    },
    {
      question: "Команды только увеличивают число. Какое условие означает «проскочили мимо цели»?",
      options: [
        { id: "A", text: "c < e: return 0" },
        { id: "B", text: "c > e: return 0" },
        { id: "C", text: "c == 0: return 0" },
        { id: "D", text: "c != e: return 1" }
      ],
      correct: "B",
      explanation: "Если команды увеличивают число, то перейдя за цель (c > e) вернуться уже нельзя — тупик, 0. Для уменьшающих команд знак был бы зеркальным: c < e."
    },
    {
      question: "Программа обязана пройти через число K. Как считаем ответ?",
      options: [
        { id: "A", text: "f(A, K) + f(K, B)" },
        { id: "B", text: "f(A, K) × f(K, B)" },
        { id: "C", text: "f(A, B) − f(A, K)" },
        { id: "D", text: "f(A, B), игнорируя K" }
      ],
      correct: "B",
      explanation: "Каждый путь A→K можно скомбинировать с любым путём K→B. Число комбинаций — это произведение: f(A,K) × f(K,B). Именно поэтому в print стоит умножение."
    },
    {
      question: "Траектория НЕ должна содержать число 10. Как это записать в f(c, e)?",
      options: [
        { id: "A", text: "Добавить в начало: if c == 10: return 0" },
        { id: "B", text: "Добавить: if c == 10: return 1" },
        { id: "C", text: "Убрать команду, которая даёт 10" },
        { id: "D", text: "Уменьшить e на 10" }
      ],
      correct: "A",
      explanation: "Запрещённое число делаем тупиком: if c == 10: return 0. Любой путь, наступивший на 10, обнулится и в сумму не войдёт."
    },
    {
      question: "Почему при условии на последовательность команд лучше хранить флаг, а не строку всех команд?",
      options: [
        { id: "A", text: "Строка занимает больше места на диске" },
        { id: "B", text: "У каждой строки своё значение → состояния не повторяются → @lru_cache не работает и решение виснет" },
        { id: "C", text: "Флаг точнее считает" },
        { id: "D", text: "Строки нельзя передавать в функцию" }
      ],
      correct: "B",
      explanation: "Кэш переиспользует одинаковые состояния. Растущая строка делает каждый вызов уникальным — кэш бесполезен. Маленький флаг (2-3 значения) даёт повторяющиеся состояния, и @lru_cache резко ускоряет счёт."
    }
  ],

  aiContext: `Задание 23 ЕГЭ — подсчёт количества программ исполнителя, переводящих число A в B. Решается короткой рекурсией (стиль Полякова).

Команда — одно действие (+1, +3, ×2 и т.п.). Программа — последовательность команд. Вопросы: «сколько программ A→B» и «сколько программ / какие числа за ровно N команд».

Базовая рекурсия (увеличивающие команды +1,+3):
def f(c,e):
    if c>e: return 0          # проскочили цель
    if c==e: return 1         # дошли — одна программа
    return f(c+1,e)+f(c+3,e)  # сумма по командам

Запрещённое число K (не содержит): в начало добавить if c==K: return 0.
Обязательное число K (содержит): ответ = f(A,K) * f(K,B). УМНОЖЕНИЕ, потому что каждый путь A→K комбинируется с любым путём K→B.
Пример 23.9: команды +1,+3, из 3 в 20, содержит 15, не содержит 10 → print(f(3,15)*f(15,20)) = 96 (24*4).

Увеличение vs уменьшение: для увеличивающих команд тупик c>e; для уменьшающих (−1, //2) всё зеркально — тупик c<e, числа убывают к цели. Знак базового условия меняется на противоположный.

Число команд: добавить счётчик k. Напр. +1,+5,×3, ровно 8 команд:
def f(c,e,k):
    if c>e or k>8: return 0
    if c==e: return k==8
    return f(c+1,e,k+1)+f(c+5,e,k+1)+f(c*3,e,k+1)
Сколько чисел из [1000,1024] достижимы из 1 за 8 команд: sum(1 for x in range(1000,1025) if f(1,x,0)) = 1.

Память о прошлой команде (напр. нельзя два умножения подряд): хранить ФЛАГ (например mult_last), а НЕ строку команд. Строка делает каждое состояние уникальным и убивает @lru_cache (на больших числах виснет). Флаг даёт повторяющиеся состояния, кэш работает:
from functools import lru_cache
@lru_cache(None)
def f(c,e,mult_last):
    if c>e: return 0
    if c==e: return 1
    total=f(c+1,e,False)+f(c+2,e,False)
    if not mult_last: total+=f(c*2,e,True)
    return total
print(f(1,15,False))  # 1545

Главное про кэш: всё, что нужно помнить, кодировать маленьким флагом/числом, а не растущей строкой.`,

  promptSuggestion: "Объясни задание 23 ЕГЭ по информатике: рекурсивный подсчёт программ исполнителя (через f(c, e))."
};
