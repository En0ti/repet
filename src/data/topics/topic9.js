import { Sheet } from 'lucide-react';

export default {
  id: "task-9",
  title: "Задание 9: Обработка таблицы",
  description: "Анализ числовых данных в таблицах — решаем кодом.",
  icon: Sheet,
  theory: `### Суть задания

Дан файл таблицы, в каждой строке — несколько чисел. Нужно найти количество строк, удовлетворяющих условию, или конкретное значение. Решаем **кодом на Python**.

### Подготовка файла

В задании файл даётся в формате Excel (.xls или .xlsx). Python не умеет читать Excel напрямую в таком формате — нужно сделать два шага:

1. Открыть файл в Excel
2. Выделить все данные (Ctrl+A), скопировать (Ctrl+C) и вставить в обычный текстовый файл .txt (например, Блокнот)

> **Обязательно:** файл .py и файл .txt должны лежать в **одной папке**. Иначе Python не найдёт файл и выдаст ошибку FileNotFoundError.

### Основа: чтение и парсинг файла

Любое решение начинается с одной и той же конструкции:

\`\`\`python
for s in open('file.txt'):
    a = [int(x) for x in s.split()]
\`\`\`

**for s in open('file.txt')** — открываем файл и перебираем его строки одну за другой. Каждая строка s — это строка текста вида '3 17 5 9 2 11'.

**s.split()** — разбивает строку по пробелам: ['3', '17', '5', '9', '2', '11']. По умолчанию разделитель — любой пробел, включая переносы строк.

**int(x) for x in s.split()** — генераторное выражение: применяем int() к каждому элементу списка, переводя строки в числа.

**a = [...]** — квадратные скобки делают из генератора список. Получаем a = [3, 17, 5, 9, 2, 11].

### Генераторы списков (list comprehension)

Это ключевой инструмент для задания 9. Позволяет создать новый список из существующего в одну строку:

\`\`\`python
[выражение for x in список if условие]
\`\`\`

Примеры:

\`\`\`python
a = [3, 17, 5, 9, 2, 11]

[x for x in a if x % 2 == 0]       # чётные: [2]
[x for x in a if a.count(x) == 1]  # встречаются ровно раз: [3, 17, 5, 9, 2, 11]
[x * 2 for x in a]                 # удвоить каждый: [6, 34, 10, 18, 4, 22]
\`\`\`

**a.count(x)** — считает сколько раз элемент x встречается в списке a.

### Полезные функции для работы со списком

\`\`\`python
len(a)       # длина списка
sum(a)       # сумма элементов
max(a)       # максимум
min(a)       # минимум
sorted(a)    # возвращает новый отсортированный список (a не меняется)
set(a)       # множество уникальных элементов: {2, 3, 5, 9, 11, 17}
len(set(a))  # количество уникальных элементов
\`\`\`

**Проверка что все числа различны:** len(a) == len(set(a)) — если уникальных столько же сколько всего, значит повторов нет.

### Тип 1: максимум, минимум, среднее

Сортируем список — это позволяет удобно обращаться к первому (минимум) и последнему (максимум) элементам через a[0] и a[-1].

\`\`\`python
k = 0
for s in open('9-159.txt'):
    a = sorted([int(x) for x in s.split()])
    if sum(a) / len(a) < (a[0] + a[-1]) / 2:
        k += 1
print(k)
\`\`\`

**a = sorted([...])** — сразу парсим и сортируем. После этого a[0] — минимальный элемент, a[-1] — максимальный.

**sum(a) / len(a)** — среднее арифметическое всех элементов строки.

**(a[0] + a[-1]) / 2** — среднее между минимумом и максимумом.

Условие: среднее арифметическое меньше, чем полусумма минимума и максимума.

> Сортировку можно применять почти всегда — она удобна и не меняет смысл данных.

### Тип 2.1: повторение чисел

Используем генераторы с a.count() чтобы отобрать элементы по количеству повторений.

\`\`\`python
for s in open('9-226.txt'):
    a = [int(x) for x in s.split()]
    a2 = [x for x in a if a.count(x) == 2]
    a1 = [x for x in a if a.count(x) == 1]
    if len(a2) == 4 and len(a1) == 3 and max(a) in a1:
        print(sum(a))
        break
\`\`\`

**a2 = [x for x in a if a.count(x) == 2]** — список элементов, которые встречаются в строке ровно 2 раза. Если число повторяется дважды — оба вхождения попадут в a2.

**a1 = [x for x in a if a.count(x) == 1]** — элементы, которые встречаются ровно 1 раз (уникальные).

**len(a2) == 4** — в a2 ровно 4 элемента, то есть два числа встречаются по 2 раза.

**len(a1) == 3** — три числа встречаются по одному разу.

**max(a) in a1** — максимальный элемент строки входит в список уникальных (то есть максимум не повторяется).

**break** — нашли первую подходящую строку, выходим. Используем когда нужна только одна строка.

### Тип 2.2: чётные и нечётные

Разбиваем элементы строки на два списка — чётные и нечётные — через генераторы.

\`\`\`python
k = 0
for s in open('9-194.txt'):
    a = [int(x) for x in s.split()]
    ch  = [x for x in a if x % 2 == 0]
    nch = [x for x in a if x % 2 != 0]
    if len(a) == len(set(a)) and len(ch) > len(nch) and sum(ch) < sum(nch):
        k += 1
print(k)
\`\`\`

**ch = [x for x in a if x % 2 == 0]** — все чётные числа строки.

**nch = [x for x in a if x % 2 != 0]** — все нечётные числа строки.

**len(a) == len(set(a))** — все числа в строке различны (нет повторов). set(a) убирает дубли, если их количество совпадает с len(a) — повторов нет.

**len(ch) > len(nch)** — чётных чисел больше чем нечётных.

**sum(ch) < sum(nch)** — но сумма чётных меньше суммы нечётных.

### Важно: читай условие внимательно

Условия бывают разные — не путай их:

- **Оба выполняются** → соединяем через **and**
- **Хотя бы одно** → через **or**
- **Только одно из двух** → через **сумму**: (условие1) + (условие2) == 1

\`\`\`python
# Только одно из двух (XOR):
if (len(ch) > 3) != (sum(a) > 100):
    k += 1
\`\`\`

### Пример (№5522, А. Рогов)

![Задача №5522](/task9-5522.png)

> В файле в каждой строке — шесть неотрицательных целых чисел. Найти количество строк, где выполнены **оба** условия:
> 1. Только одно число повторяется трижды, остальные не повторяются.
> 2. Утроенная сумма неповторяющихся чисел не больше произведения повторяющихся (то есть куба повторяющегося числа).

\`\`\`python
k = 0
for s in open('9-170.txt'):
    a = [int(x) for x in s.split()]
    a1 = [x for x in a if a.count(x) == 1]
    a3 = [x for x in a if a.count(x) == 3]
    if len(a3) == 3 and len(a1) == 3 and 3 * sum(a1) <= a3[0] ** 3:
        k += 1
print(k)
\`\`\`

**Разбор:**

**a1** — числа, встречающиеся ровно 1 раз (неповторяющиеся).

**a3** — числа, встречающиеся ровно 3 раза (повторяющееся трижды число — все три вхождения).

**len(a3) == 3** — ровно одно число повторяется трижды (три вхождения = 3 элемента в a3).

**len(a1) == 3** — остальные три числа уникальны. Итого в строке 6 чисел: 3 + 3 = 6.

**3 * sum(a1)** — утроенная сумма неповторяющихся.

**a3[0] ** 3** — куб повторяющегося числа. a3[0] — любой элемент из a3 (все они одинаковые). ** — возведение в степень.

**Ответ: 134**`,

  practice: [
    {
      question: "Что делает строка `a = [int(x) for x in s.split()]`?",
      options: [
        { id: "A", text: "Разбивает строку s на символы и считает их." },
        { id: "B", text: "Разбивает строку по пробелам и преобразует каждую часть в число, создавая список целых чисел." },
        { id: "C", text: "Открывает файл и читает первую строку." },
        { id: "D", text: "Сортирует числа в строке по возрастанию." }
      ],
      correct: "B",
      explanation: "s.split() разбивает строку '3 17 5 9' по пробелам: ['3', '17', '5', '9']. int(x) for x in ... применяет int() к каждому элементу. Квадратные скобки собирают результат в список: [3, 17, 5, 9]."
    },
    {
      question: "Как получить список только чётных чисел из списка a?",
      options: [
        { id: "A", text: "[x for x in a if x % 2]" },
        { id: "B", text: "[x for x in a if x % 2 == 0]" },
        { id: "C", text: "[x % 2 for x in a]" },
        { id: "D", text: "even(a)" }
      ],
      correct: "B",
      explanation: "x % 2 == 0 — остаток от деления на 2 равен 0, то есть число чётное. Вариант A (if x % 2) — это нечётные, потому что для нечётных x % 2 равно 1, что истинно. Не путай!"
    },
    {
      question: "Что проверяет условие `len(a) == len(set(a))`?",
      options: [
        { id: "A", text: "Что все числа в списке чётные." },
        { id: "B", text: "Что список не пустой." },
        { id: "C", text: "Что все числа в списке различны (нет повторений)." },
        { id: "D", text: "Что сумма равна длине списка." }
      ],
      correct: "C",
      explanation: "set(a) убирает дубликаты. Если len(set(a)) == len(a) — значит дубликатов не было, все элементы уникальны. Например: a=[1,2,3] → set={1,2,3}, оба len=3. Но a=[1,2,2] → set={1,2}, len(a)=3 ≠ len(set)=2."
    },
    {
      question: "В условии написано «выполняется ТОЛЬКО ОДНО из двух условий». Как это записать в Python?",
      options: [
        { id: "A", text: "if cond1 and cond2" },
        { id: "B", text: "if cond1 or cond2" },
        { id: "C", text: "if cond1 + cond2 == 1" },
        { id: "D", text: "if not cond1 and not cond2" }
      ],
      correct: "C",
      explanation: "cond1 + cond2 == 1: истинно только когда одно условие True, другое False. Если оба True или оба False — результат False. Это точно соответствует «только одно из двух»."
    },
    {
      question: "Список a = [5, 3, 5, 7, 5, 3]. Что вернёт [x for x in a if a.count(x) == 2]?",
      options: [
        { id: "A", text: "[3, 3]" },
        { id: "B", text: "[5, 5, 5]" },
        { id: "C", text: "[3]" },
        { id: "D", text: "[5, 3]" }
      ],
      correct: "A",
      explanation: "a.count(3) = 2, a.count(5) = 3, a.count(7) = 1. Условие a.count(x) == 2 выполняется только для 3. В списке два вхождения тройки — оба попадут в результат: [3, 3]."
    }
  ],

  aiContext: `Это задание 9 ЕГЭ по информатике — обработка числовой таблицы кодом на Python.

Основа любого решения:
for s in open('file.txt'):
    a = [int(x) for x in s.split()]

Типы задач:
1. Макс/мин/среднее → sorted(), a[0], a[-1], sum(a)/len(a)
2.1 Повторения → a.count(x)==N, генераторы a2=[x for x in a if a.count(x)==2]
2.2 Чёт/нечёт → ch=[x for x in a if x%2==0], nch=[x for x in a if x%2!=0]

Ключевые инструменты:
- len(set(a)) == len(a) — все числа различны
- a.count(x) — сколько раз x встречается в a
- sum/max/min/sorted работают на списках напрямую
- break — выйти после первой подходящей строки

Условия:
- Оба → and
- Хотя бы одно → or
- Только одно → cond1 + cond2 == 1

Частые ошибки:
- Путают x%2==0 (чётное) и x%2 (нечётное — это тоже истинно при нечётном x)
- Забывают что a.count(x)==2 вернёт ОБА вхождения, поэтому len будет 2, а не 1
- Не используют break когда нужна только одна строка`,

  promptSuggestion: "Объясни задание 9 ЕГЭ по информатике: как обрабатывать таблицу через Python."
};
