import { Cpu } from 'lucide-react';

export default {
  id: "task-5",
  title: "Задание 5: Анализ алгоритмов",
  description: "Системы счисления, срезы, алгоритмы над числами.",
  icon: Cpu,
  theory: `### Что требуется

Дан алгоритм, работающий с числами — обычно переводит в другую систему счисления, преобразует цифры, считает сумму. Нужно найти конкретное значение: максимальное/минимальное N, количество чисел с нужным свойством и т.д.

Задание решается **кодом на Python**: пишем алгоритм из условия и перебираем числа.

### Перевод в другие системы счисления

#### Встроенные функции

\`\`\`python
bin(n)[2:]   # двоичная (основание 2)
oct(n)[2:]   # восьмеричная (основание 8)
hex(n)[2:]   # шестнадцатеричная (основание 16)
\`\`\`

\`[2:]\` — срез, отбрасывает префикс \`0b\` / \`0o\` / \`0x\`, который Python добавляет автоматически.

#### Через f-строку (короче и удобнее)

\`\`\`python
f'{n:b}'   # двоичная
f'{n:o}'   # восьмеричная
f'{n:h}'   # шестнадцатеричная
\`\`\`

F-строка сразу возвращает чистую запись без префикса — \`[2:]\` не нужен.

#### Перевод обратно в десятичную

\`\`\`python
int('1011', 2)    # из двоичной → 11
int('17', 8)      # из восьмеричной → 15
int('ff', 16)     # из шестнадцатеричной → 255
int(s, n)         # универсально: строка s в основании n
\`\`\`

#### Перевод в произвольное основание (своя функция)

Встроенных функций для оснований 3, 5, 7 и т.д. нет. Пишем сами:

\`\`\`python
def cc(x, n):
    s = ''
    while x > 0:
        s = str(int(x % n)) + s
        x = x // n
    return s
\`\`\`

Принцип: берём остаток от деления на основание, добавляем **слева** к строке, делим нацело. Повторяем пока число не обнулится.

Пример: \`cc(13, 3)\` → \`'111'\` (13 = 1·9 + 1·3 + 1·1)

### Срезы строк

Срез \`s[x:y:z]\`:
- \`x\` — откуда начинаем (включительно)
- \`y\` — до куда идём (не включая)
- \`z\` — шаг

\`\`\`python
s = '1011010'
s[2:]      # '11010'  — с позиции 2 до конца
s[:3]      # '101'    — первые 3 символа
s[1:5]     # '0110'   — с 1 по 4
s[::-1]    # '0101101' — разворот строки
s[2:]      # отбросить первые 2 символа (как при [2:] после bin())
\`\`\`

Отрицательные индексы считают с конца: \`s[-1]\` — последний символ, \`s[:-1]\` — всё кроме последнего.

### Сумма цифр числа

#### Базовый вариант (через цикл по строке)

\`\`\`python
sm = 0
for c in str(n):
    sm += int(c)
\`\`\`

#### Продвинутый вариант (однострочник)

\`\`\`python
sum(map(int, str(n)))
\`\`\`

\`str(n)\` → строка цифр → \`map(int, ...)\` применяет \`int\` к каждому символу → \`sum(...)\` суммирует.

Для цифр в другой системе счисления — сначала переводим:

\`\`\`python
sum(map(int, bin(n)[2:]))   # сумма битов в двоичной записи
sum(map(int, f'{n:b}'))     # то же самое через f-строку
\`\`\`

### Пример (ЕГЭ-2022)

![Задача ЕГЭ-2022](/task5-example.png)

> На вход подаётся натуральное число N. Алгоритм:
> 1. Строится двоичная запись N.
> 2. Если сумма цифр **чётная** → дописать **0** справа, два левых разряда заменить на **10**.
>    Если сумма цифр **нечётная** → дописать **1** справа, два левых разряда заменить на **11**.
> 3. Результат перевести в десятичную.
>
> Найти **максимальное N**, после обработки которого R < 35.

#### Разбор условия

Разберём на примере из задачи:
- N=6 → двоичная \`110\`, сумма цифр = 1+1+0 = **2 (чётная)** → дописать 0 справа: \`1100\`, заменить два левых на \`10\`: \`1000\` → R = 8 ✓
- N=4 → двоичная \`100\`, сумма цифр = 1 (**нечётная**) → дописать 1 справа: \`1001\`, заменить два левых на \`11\`: \`1101\` → R = 13 ✓

#### Код решения

\`\`\`python
for n in range(1000, 1, -1):
    b = f'{n:b}'
    if b.count('1') % 2 == 0:
        b = '10' + b[2:] + '0'
    else:
        b = '11' + b[2:] + '1'
    R = int(b, 2)
    if R < 35:
        print(n)
        break
\`\`\`

#### Разбор кода строка за строкой

\`for n in range(1000, 1, -1)\` — перебираем N от 1000 вниз до 2, ищем максимальное подходящее.

\`b = f'{n:b}'\` — двоичная запись N без префикса.

\`b.count('1') % 2 == 0\` — сумма цифр двоичной записи = количество единиц. Чётная ↔ остаток от деления на 2 равен 0.

\`b = '10' + b[2:] + '0'\` — разбираем по шагам:
- \`b[2:]\` — строка без первых двух символов (убираем два левых разряда)
- \`'10' + b[2:]\` — ставим \`10\` вместо них слева
- \`+ '0'\` — дописываем \`0\` справа

\`int(b, 2)\` — переводим результат из двоичной в десятичную.

\`break\` — нашли первое (максимальное) N сверху, выходим.

#### Когда break работает, а когда нет

**Ищем N** (максимальное/минимальное) — \`break\` подходит. Мы сами контролируем порядок перебора: идём сверху вниз → первое подходящее N и есть максимальное.

**Ищем R** — \`break\` опасен. R не обязан монотонно возрастать или убывать при увеличении N. Например, при N=10 может быть R=40, при N=11 — R=20, при N=12 — R=45. Нельзя угадать, когда R достигнет нужного значения. Правильный подход — собирать все подходящие R в список и брать максимум/минимум:

\`\`\`python
results = []
for n in range(1, 1001):
    b = f'{n:b}'
    if b.count('1') % 2 == 0:
        b = '10' + b[2:] + '0'
    else:
        b = '11' + b[2:] + '1'
    R = int(b, 2)
    if R < 35:
        results.append(R)
print(max(results))
\`\`\``,

  practice: [
    {
      question: "Что вернёт `f'{13:b}'`?",
      options: [
        { id: "A", text: "`'0b1101'`" },
        { id: "B", text: "`'1101'`" },
        { id: "C", text: "`'13'`" },
        { id: "D", text: "`'d'`" }
      ],
      correct: "B",
      explanation: "F-строка с форматом `:b` возвращает двоичное представление числа без префикса `0b`. 13 = 8+4+1 = 1101 в двоичной. В отличие от `bin(13)`, который вернул бы `'0b1101'`, f-строка сразу чистая."
    },
    {
      question: "Что делает `bin(n)[2:]`?",
      options: [
        { id: "A", text: "Возвращает двоичную запись N, начиная со второго бита." },
        { id: "B", text: "Возвращает двоичную запись N без префикса `0b`." },
        { id: "C", text: "Возвращает только два первых бита числа N." },
        { id: "D", text: "Переводит строку из двоичной в десятичную." }
      ],
      correct: "B",
      explanation: "`bin(n)` возвращает строку вида `'0b1011'`. Срез `[2:]` отбрасывает первые два символа — префикс `'0b'`. Остаётся чистая двоичная запись `'1011'`. То же самое даёт `f'{n:b}'` без среза."
    },
    {
      question: "Нужно посчитать сумму цифр числа N в десятичной записи. Какой вариант правильный?",
      options: [
        { id: "A", text: "`sum(n)`" },
        { id: "B", text: "`sum(map(int, str(n)))`" },
        { id: "C", text: "`sum(map(int, bin(n)))`" },
        { id: "D", text: "`int(sum(str(n)))`" }
      ],
      correct: "B",
      explanation: "`str(n)` превращает число в строку цифр, например `'1234'`. `map(int, ...)` применяет `int` к каждому символу: `[1, 2, 3, 4]`. `sum(...)` складывает. Вариант А не работает — число не итерируемо. Вариант С считает сумму битов, а не десятичных цифр."
    },
    {
      question: "Строка `s = '1011010'`. Что вернёт `s[::-1]`?",
      options: [
        { id: "A", text: "`'010'`" },
        { id: "B", text: "`'101'`" },
        { id: "C", text: "`'0101101'`" },
        { id: "D", text: "`'1011010'`" }
      ],
      correct: "C",
      explanation: "Срез `[::-1]` означает: идти по всей строке с шагом -1, то есть в обратном порядке. Это стандартный способ развернуть строку в Python. `'1011010'` задом наперёд — `'0101101'`."
    },
    {
      question: "Функция `cc(x, n)` переводит число в произвольное основание. Что вернёт `cc(10, 3)`?",
      options: [
        { id: "A", text: "`'101'`" },
        { id: "B", text: "`'22'`" },
        { id: "C", text: "`'12'`" },
        { id: "D", text: "`'30'`" }
      ],
      correct: "A",
      explanation: "Переводим 10 в тройичную последовательным делением: 10 % 3 = 1 (остаток), 10 // 3 = 3; 3 % 3 = 0, 3 // 3 = 1; 1 % 3 = 1, 1 // 3 = 0. Функция cc добавляет каждый остаток слева: получаем '101'. Проверка: 1·9 + 0·3 + 1·1 = 10 ✓. Ответ — '101'."
    }
  ],

  aiContext: `Это задание 5 ЕГЭ по информатике — анализ алгоритмов, работающих с числами в разных системах счисления.

Задание решается кодом на Python: берём алгоритм из условия, реализуем его и перебираем числа.

Ключевые инструменты Python:
- Перевод в СС: bin(n)[2:], oct(n)[2:], hex(n)[2:] или f'{n:b}', f'{n:o}', f'{n:h}'
- Обратно в десятичную: int(s, основание)
- В произвольное основание (3, 5, 7...): функция cc(x, n) через цикл с делением
- Срезы строк: s[x:y:z], x включительно, y не включая, z шаг; s[::-1] разворот
- Сумма цифр: sum(map(int, str(n))) или цикл sm += int(c) for c in str(n)
- Сумма битов: sum(map(int, f'{n:b}')) или b.count('1')

Типичная схема решения:
1. Перебор range(начало, конец, шаг)
2. Реализуем алгоритм из условия
3. Проверяем условие задачи
4. При нахождении ответа — break (если нужен первый/максимальный)

Частые ошибки:
- Забывают [2:] при использовании bin/oct/hex (получают '0b...' вместо чистой строки)
- Путают направление перебора: для максимального N идут сверху вниз (range(1000, 1, -1))
- В срезах путают включительно/не включая для правой границы
- При замене символов в строке забывают что строки в Python неизменяемы — нужно собирать новую строку`,

  promptSuggestion: "Объясни задание 5 ЕГЭ по информатике: как писать алгоритмы на Python для работы с числами в разных системах счисления."
};
