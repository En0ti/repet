import { Hash } from 'lucide-react';

export default {
  id: "task-8",
  title: "Задание 8: Комбинаторика",
  description: "Подсчёт вариантов через itertools.",
  icon: Hash,
  theory: `### Суть задания

Нужно посчитать количество слов, чисел или последовательностей, удовлетворяющих условию. Всё решается перебором через **itertools** — забываем про формулы, пишем код.

### Тип 1: символы повторяются → product

Если в условии сказано «буквы могут повторяться» — используем **product** с параметром **repeat**.

\`\`\`python
from itertools import *

k = 0
for x in product('ясновидец', repeat=5):
    s = ''.join(x)
    if s[0] in 'снвдц' and s[-1] in 'яоие' and s.count(s[0]) == 1 and s.count(s[-1]) == 1:
        k += 1
print(k)
\`\`\`

**Разбор по строкам:**

**from itertools import*** — подключаем всю библиотеку itertools. После этого можно писать просто product, permutations и т.д. без префикса.

**k = 0** — счётчик подходящих вариантов. Каждый раз когда условие выполняется — увеличиваем на 1.

**for x in product('ясновидец', repeat=5)** — перебираем все возможные последовательности длиной 5 из букв строки 'ясновидец', причём буквы **могут повторяться**. Каждый x — это кортеж, например ('я', 'с', 'н', 'о', 'в').

**s = ''.join(x)** — склеиваем кортеж в строку. ('я','с','н','о','в') → 'яснов'. Дальше удобно работать со строкой.

**s[0] in 'снвдц'** — проверяем, входит ли первый символ в множество согласных. Работает как «s[0] == 'с' или s[0] == 'н' или ...», только короче.

**s[-1] in 'яоие'** — последний символ строки (индекс -1 = с конца) должен быть гласной.

**s.count(s[0]) == 1** — первая буква встречается в слове ровно один раз. count() считает количество вхождений подстроки.

**s.count(s[-1]) == 1** — последняя буква тоже встречается ровно один раз.

> **repeat** есть только у product. У permutations его нет.

### Тип 2: символы не повторяются → permutations

Если «все символы различны» или «каждая буква не более одного раза» — используем **permutations**.

\`\`\`python
from itertools import *

k = 0
for x in permutations('магастр', 5):
    s = ''.join(x)
    if s.count('а') <= 1:
        k += 1
print(k)
\`\`\`

**Что нового:**

**permutations('магастр', 5)** — генерирует все перестановки из 7 букв длиной 5, где **каждая буква встречается не более одного раза**. Второй аргумент — длина слова. Если не указать — берётся вся строка целиком.

Всё остальное (k = 0, join, count, print) — то же самое что в типе 1.

> Заметь: в строке 'магастр' буква 'а' встречается дважды. permutations воспринимает их как **разные** символы (по позиции), поэтому слова с двумя 'а' будут появляться — и условие s.count('а') <= 1 их отсекает.

#### Когда в алфавите дубли + permutations → set()

Если строка содержит повторяющиеся буквы и нам нужны **уникальные** перестановки — оборачиваем в **set()**, чтобы убрать дубликаты:

\`\`\`python
from itertools import *

k = 0
for x in set(permutations('трата')):
    k += 1
print(k)
\`\`\`

**set(permutations('трата'))** — без set() перестановки 'трата' выдадут одинаковые слова (например 'трата' появится несколько раз из-за двух 'т' и двух 'а'). set() оставляет только уникальные кортежи.

### Тип 3: порядковый номер в словаре → sorted + счётчик

Если задача про **порядок** (например, «на каком месте стоит слово»), перебор должен идти строго в алфавитном порядке. Для этого оборачиваем алфавит в **sorted()** — он сортирует буквы автоматически.

\`\`\`python
from itertools import *

k = 0
k2 = 0
for x in product(sorted('бжоуфц'), repeat=4):
    s = ''.join(x)
    k += 1
    if k % 2 == 0 and s[0] + s[1] == 'жо':
        k2 += 1
print(k2)
\`\`\`

**Что нового:**

**sorted('бжоуфц')** — сортирует буквы по алфавиту перед перебором. product тогда генерирует слова в лексикографическом порядке: сначала 'бббб', потом 'бббж', потом 'бббо'...

**k += 1** стоит **до** проверки условия — это порядковый номер текущего слова в словаре.

**k2** — отдельный счётчик для слов, которые удовлетворяют условию. k и k2 — разные вещи.

**k % 2 == 0** — слово стоит на чётной позиции.

### Лайфхак: замена символов для чередования

Если нужно проверять чередование (чётная/нечётная цифра) — можно **подменить символы** прямо в строке. Python будет считать их разными, перестановки не потеряются.

Пример: шестнадцатеричные цифры, чётные кодируем как '0' и '2', нечётные как '1':

\`\`\`python
from itertools import *

k = 0
for x in permutations('0121212121212121', 3):
    s = ''.join(x)
    if s[0] != '0':
        s = s.replace('0', '2')
        if '02' not in s and '20' not in s and '22' not in s and '00' not in s and '11' not in s:
            k += 1
print(k)
\`\`\`

**s.replace('0', '2')** — заменяем все '0' на '2'. Теперь в строке только '1' и '2' — нечётное и чётное. Проверяем что никакие два одинаковых типа не стоят рядом ('11', '22') и нет запрещённых пар ('02', '20', '00').

### Главное правило для чисел

Если составляем **числа** — число не может начинаться с нуля:

\`\`\`python
if s[0] != '0':
    k += 1
\`\`\`

Всегда добавляй это условие когда задача про числа, а не слова.

### Пример 1 — слова (А. Куканова)

![Пример 1](/task8-example1.png)

> Лена составляет 5-буквенные слова из букв Я, С, Н, О, В, И, Д, Е, Ц. Слово начинается с согласной и заканчивается гласной. Первая и последняя буквы встречаются только по одному разу. Остальные буквы могут повторяться. Сколько слов?

Алфавит: ясновидец (9 букв). Символы могут повторяться → **product**.

\`\`\`python
from itertools import *

k = 0
for x in product('ясновидец', repeat=5):
    s = ''.join(x)
    if s[0] in 'снвдц' and s[-1] in 'яоие' and s.count(s[0]) == 1 and s.count(s[-1]) == 1:
        k += 1
print(k)  # 6860
\`\`\`

**Ответ: 6860**

### Пример 2 — шестнадцатеричные числа (ЕГЭ-2023)

![Пример 2](/task8-example2.png)

> Сколько существует шестнадцатеричных трёхзначных чисел, в которых все цифры различны и никакие две чётные или две нечётные цифры не стоят рядом?

Цифры: 0–9, A–F. Чётные: 0,2,4,6,8,A,C,E (8 штук). Нечётные: 1,3,5,7,9,B,D,F (8 штук). Все различны → **permutations**. Кодируем чётные как '0'/'2', нечётные как '1'.

\`\`\`python
from itertools import *

k = 0
for x in permutations('0121212121212121', 3):
    s = ''.join(x)
    if s[0] != '0':
        s = s.replace('0', '2')
        if '02' not in s and '20' not in s and '22' not in s and '00' not in s and '11' not in s:
            k += 1
print(k)  # 840
\`\`\`

**Ответ: 840**`,

  practice: [
    {
      question: "Символы в слове **могут повторяться**. Какую функцию из itertools использовать?",
      options: [
        { id: "A", text: "permutations" },
        { id: "B", text: "combinations" },
        { id: "C", text: "product с параметром repeat" },
        { id: "D", text: "product без repeat" }
      ],
      correct: "C",
      explanation: "product('abc', repeat=3) генерирует все последовательности длины 3 с повторениями. Параметр repeat есть только у product — у permutations и combinations его нет."
    },
    {
      question: "Что делает ''.join(x) если x = ('я', 'с', 'н', 'о', 'в')?",
      options: [
        { id: "A", text: "Возвращает список букв." },
        { id: "B", text: "Склеивает кортеж в строку 'яснов'." },
        { id: "C", text: "Считает количество элементов." },
        { id: "D", text: "Сортирует буквы по алфавиту." }
      ],
      correct: "B",
      explanation: "''.join(x) склеивает все элементы итерируемого объекта в одну строку, используя '' (пустую строку) как разделитель. ('я','с','н','о','в') → 'яснов'. Это стандартный способ превратить кортеж из символов в строку."
    },
    {
      question: "Строка 'трата' содержит повторяющиеся буквы. Как получить только уникальные перестановки?",
      options: [
        { id: "A", text: "Использовать product вместо permutations." },
        { id: "B", text: "for x in set(permutations('трата'))" },
        { id: "C", text: "Добавить if len(set(s)) == len(s)" },
        { id: "D", text: "Убрать повторяющиеся буквы вручную." }
      ],
      correct: "B",
      explanation: "set() убирает дубликаты. Без него permutations выдаст одинаковые перестановки из-за повторных букв. set(permutations('трата')) оставит только уникальные кортежи."
    },
    {
      question: "Составляем 3-значные числа из цифр '0123'. Какое условие нужно добавить?",
      options: [
        { id: "A", text: "if s[-1] != '0'" },
        { id: "B", text: "if s[0] != '0'" },
        { id: "C", text: "if '0' not in s" },
        { id: "D", text: "Ничего не нужно." }
      ],
      correct: "B",
      explanation: "Числа не начинаются с нуля. if s[0] != '0' отсекает варианты вроде '012' или '031'. Условие if '0' not in s — лишнее: оно выбросит числа где 0 стоит в середине или конце, что не запрещено."
    },
    {
      question: "Зачем использовать sorted() на алфавите при переборе?",
      options: [
        { id: "A", text: "Чтобы ускорить программу." },
        { id: "B", text: "Чтобы перебор шёл в алфавитном порядке — нужно когда задача про порядковый номер слова в словаре." },
        { id: "C", text: "Чтобы убрать повторяющиеся буквы." },
        { id: "D", text: "product работает только с отсортированными строками." }
      ],
      correct: "B",
      explanation: "Если нужен порядковый номер слова в словаре, перебор должен идти строго по алфавиту. sorted() сортирует буквы, и тогда product генерирует слова лексикографически. Счётчик k при этом = порядковый номер текущего слова."
    }
  ],

  aiContext: `Это задание 8 ЕГЭ по информатике — комбинаторика через перебор на Python с itertools.

Три типа:
1. Символы повторяются → product('алфавит', repeat=длина)
2. Символы не повторяются → permutations('алфавит', длина)
3. Порядковый номер в словаре → sorted() на алфавите + счётчик k

Ключевые приёмы:
- repeat только у product, у permutations его нет
- Если в алфавите дубли и нужны перестановки → set(permutations(...))
- Числа не начинаются с нуля: if s[0] != '0'
- Для чередования чётных/нечётных → s.replace() для подмены символов
- s.count(s[0]) == 1 — символ встречается ровно раз
- s[0] in 'снвдц' — проверка принадлежности к множеству

Шаблон:
from itertools import *
k = 0
for x in product/permutations('алфавит', ...):
    s = ''.join(x)
    if условие:
        k += 1
print(k)

Частые ошибки:
- Пишут repeat у permutations (его нет)
- Забывают set() когда в алфавите дубли + permutations
- Забывают s[0] != '0' для числовых задач
- Не используют sorted() когда нужен порядковый номер`,

  promptSuggestion: "Объясни задание 8 ЕГЭ по информатике: как решать комбинаторику через itertools."
};
