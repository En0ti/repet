import { Repeat } from 'lucide-react';

export default {
  id: "task-16",
  title: "Задание 16: Рекурсивные алгоритмы",
  description: "Анализ и подсчёт значений рекурсивных функций.",
  icon: Repeat,
  theory: `### Суть задания

Даны одна или больше рекурсивных функций с несколькими ветками. Нужно найти значение F(N) для большого числа N.

Наивная рекурсия не сработает — Python упрётся в лимит вызовов. Решение: **кэширование + прогонка**.

### Два правила

**Правило 1:** \`@lru_cache(None)\` ставим **перед каждой** функцией — без исключений.

**Правило 2:** \`setrecursionlimit\` **НЕ используем** — вместо этого делаем прогонку.

### Как работает кэширование

\`@lru_cache(None)\` запоминает результат каждого вызова функции. Если f(100) уже вычислялась — она не будет считаться заново, а вернёт готовый ответ мгновенно.

Проблема: при первом вызове f(75000) Python пытается раскрыть всю рекурсию сразу → переполнение стека.

Решение: **прогонка** — вызываем функцию заранее в цикле, постепенно заполняя кэш. Когда дойдём до нужного значения — все промежуточные уже в кэше, стек не переполняется.

### Направление прогонки

\`\`\`svg
<svg viewBox="0 0 560 190" xmlns="http://www.w3.org/2000/svg" style="max-width:100%;height:auto">
  <defs><style>
    .bg{fill:#0f172a}.hdr{fill:#065f46}.r0{fill:#1e293b}.r1{fill:#0f172a}
    .ht{fill:#fff;font:bold 12px sans-serif;text-anchor:middle;dominant-baseline:middle}
    .lbl{fill:#cbd5e1;font:11px sans-serif;dominant-baseline:middle}
    .gr{fill:#34d399;font:bold 11px monospace;dominant-baseline:middle}
    .rd{fill:#f87171;font:bold 11px monospace;dominant-baseline:middle}
    .sep{stroke:#334155;stroke-width:0.5}
    .arr{fill:none;stroke-width:2}
  </style>
  <marker id="ag" markerWidth="7" markerHeight="7" refX="5" refY="3" orient="auto"><path d="M0,0 L0,6 L7,3 z" fill="#34d399"/></marker>
  <marker id="ar" markerWidth="7" markerHeight="7" refX="5" refY="3" orient="auto"><path d="M0,0 L0,6 L7,3 z" fill="#f87171"/></marker>
  </defs>

  <rect width="560" height="190" class="bg" rx="8"/>

  <!-- header -->
  <rect width="560" height="30" class="hdr"/>
  <text x="140" y="15" class="ht">Рекурсия идёт...</text>
  <text x="350" y="15" class="ht">Прогонка (цикл)</text>
  <line x1="280" y1="0" x2="280" y2="190" class="sep"/>

  <!-- row 1: minus -->
  <rect y="30" width="560" height="76" class="r0"/>
  <text x="50" y="52" fill="#f87171" font-size="22" font-family="monospace" text-anchor="middle" dominant-baseline="middle">↓</text>
  <text x="140" y="48" class="lbl">С минусом: f(n-2), f(n-5)</text>
  <text x="140" y="65" class="lbl">Аргумент уменьшается →</text>
  <text x="140" y="82" class="lbl">нужны малые значения первыми</text>
  <text x="350" y="55" class="gr">for i in range(1, N+1):</text>
  <text x="350" y="73" class="gr">    f(i)</text>
  <text x="350" y="91" fill="#94a3b8" font-size="10" font-family="sans-serif" dominant-baseline="middle">от малых к большим →</text>

  <!-- row 2: plus -->
  <rect y="106" width="560" height="76" class="r1"/>
  <text x="50" y="128" fill="#34d399" font-size="22" font-family="monospace" text-anchor="middle" dominant-baseline="middle">↑</text>
  <text x="140" y="124" class="lbl">С плюсом: g(n+3)</text>
  <text x="140" y="141" class="lbl">Аргумент увеличивается →</text>
  <text x="140" y="158" class="lbl">нужны большие значения первыми</text>
  <text x="350" y="131" class="rd">for i in range(N, 0, -1):</text>
  <text x="350" y="149" class="rd">    g(i)</text>
  <text x="350" y="167" fill="#94a3b8" font-size="10" font-family="sans-serif" dominant-baseline="middle">от больших к малым ←</text>

  <rect width="560" height="190" fill="none" stroke="#1e293b" stroke-width="1.5" rx="8"/>
</svg>
\`\`\`

**Почему так?** При прогонке нужно идти в сторону **базового случая**. Если f(n) вызывает f(n-5), то чтобы f(100) опиралась на готовый кэш f(95) — нужно сначала вычислить f(95), то есть идти снизу вверх.

### Порядок прогонок: сначала независимая функция

Если функция A вызывает функцию B, то B — **независимая**. Прогоняем B первой, потом A.

> В примере: f зависит от g (вызывает g(n-2)). Значит сначала прогоняем **g**, потом **f**.

### Разбор примера

![Задача: рекурсивные функции F и G](/tasks/16/example.png)

\`\`\`python
from functools import *

@lru_cache(None)
def f(n):
    if n < 30000: return n + g(n - 2)
    return n + f(n - 5)

@lru_cache(None)
def g(n):
    if n < 30000: return 10 + n + g(n + 3)
    return n ** 2

for i in range(75000, 1, -1): g(i)   # g идёт вверх (+3) → прогоняем сверху вниз
for i in range(1, 75000):     f(i)   # f идёт вниз (-5, -2) → прогоняем снизу вверх

print(f(75000))
\`\`\`

**\`from functools import *\`** — импортирует \`lru_cache\`. Всегда первой строкой.

**\`@lru_cache(None)\`** — декоратор кэширования. None означает неограниченный размер кэша. Ставим перед каждой функцией.

**Разбор ветвей функции g:**
- \`if n < 30000: return 10 + n + g(n + 3)\` — аргумент растёт (+3), поэтому g идёт **вверх**
- \`return n ** 2\` — базовый случай при n ≥ 30000 (не рекурсивный)

**Разбор ветвей функции f:**
- \`if n < 30000: return n + g(n - 2)\` — вызывает g с меньшим аргументом
- \`return n + f(n - 5)\` — аргумент уменьшается (-5), f идёт **вниз**

**Прогонка g:** \`for i in range(75000, 1, -1): g(i)\` — от 75000 до 2. Сначала вычисляем g(75000) = 75000² (базовый случай, без рекурсии), потом g(74999) опирается на g(75002) — но он тоже базовый. Цепочка постепенно заполняет кэш.

**Прогонка f:** \`for i in range(1, 75000): f(i)\` — от 1 до 74999. f(1) зависит от g(-1) (уже в кэше) и т.д.

**Важно:** Прогонки не нужно прерывать по условию или менять диапазон — просто запускаем для достаточно большого диапазона, охватывающего все нужные значения.`,

  practice: [
    {
      question: "Почему в задании 16 нельзя просто написать f(75000) без прогонки?",
      options: [
        { id: "A", text: "Python не поддерживает рекурсию" },
        { id: "B", text: "Рекурсия раскроется в один огромный стек вызовов и Python выдаст ошибку переполнения" },
        { id: "C", text: "lru_cache не работает без прогонки" },
        { id: "D", text: "Функция вернёт неверный результат" }
      ],
      correct: "B",
      explanation: "Python по умолчанию ограничивает глубину стека ~1000 вызовов. f(75000) → f(74995) → ... создаст тысячи вложенных вызовов и вызовет RecursionError. Прогонка решает это: заполняем кэш итеративно, и каждый следующий вызов уже опирается на готовые значения."
    },
    {
      question: "Функция h(n) = h(n+2) при n < 1000, иначе = n. В какую сторону делать прогонку?",
      options: [
        { id: "A", text: "for i in range(1, N+1): h(i) — снизу вверх" },
        { id: "B", text: "for i in range(N, 0, -1): h(i) — сверху вниз" },
        { id: "C", text: "Направление не важно" },
        { id: "D", text: "Прогонка не нужна, так как базовый случай простой" }
      ],
      correct: "B",
      explanation: "h идёт вверх (+2), поэтому нужны большие значения в кэше первыми. Прогоняем сверху вниз: range(N, 0, -1). Тогда h(999) уже найдёт h(1001) в кэше (базовый случай)."
    },
    {
      question: "Есть две функции: p(n) вызывает q(n-1), q(n) не вызывает p. Что прогоняем первым?",
      options: [
        { id: "A", text: "p, потому что она главная" },
        { id: "B", text: "q, потому что она независимая (p зависит от q)" },
        { id: "C", text: "Порядок не важен при lru_cache" },
        { id: "D", text: "Обе одновременно" }
      ],
      correct: "B",
      explanation: "p зависит от q. Когда p(i) будет вычисляться в прогонке, ей нужно найти q(i-1) уже в кэше. Значит q должна быть прогнана первой — иначе прогонка p снова запустит рекурсию q."
    },
    {
      question: "Сколько раз @lru_cache(None) нужно написать, если в задаче две функции f и g?",
      options: [
        { id: "A", text: "Один раз — перед f" },
        { id: "B", text: "Один раз — перед главной функцией" },
        { id: "C", text: "Два раза — перед каждой функцией" },
        { id: "D", text: "Не нужен, если есть прогонка" }
      ],
      correct: "C",
      explanation: "@lru_cache(None) ставится перед КАЖДОЙ рекурсивной функцией. Если забыть про g — её результаты не будут кэшироваться, и при каждом вызове g из f она будет запускать новую рекурсию."
    }
  ],

  aiContext: `Задание 16 ЕГЭ — рекурсивные алгоритмы, вычисление f(N) для большого N.

Правила:
1. @lru_cache(None) перед КАЖДОЙ функцией
2. setrecursionlimit НЕ использовать
3. Прогонка (warmup) заполняет кэш итеративно

Направление прогонки:
- Рекурсия с минусом (n-k): прогонка снизу вверх — range(1, N+1)
- Рекурсия с плюсом (n+k): прогонка сверху вниз — range(N, 0, -1)

Порядок прогонок: сначала независимая функция (та, от которой зависят другие).

Шаблон решения:
from functools import *
@lru_cache(None)
def f(n): ...
@lru_cache(None)
def g(n): ...
for i in range(N, 0, -1): g(i)   # если g идёт вверх
for i in range(1, N+1): f(i)     # если f идёт вниз
print(f(N))`,

  promptSuggestion: "Объясни задание 16 ЕГЭ по информатике: рекурсия и кэширование."
};
